// CivicVision AI — frontend configuration
//
// Leave this empty to run fully offline using the built-in in-browser
// classifier (no backend needed — good for GitHub Pages / static hosting).
//
// Once you deploy the FastAPI backend (see /backend), paste its public
// URL here, e.g. "https://civicvision-backend.onrender.com"
// Do NOT put an API key here — this file is public. The Anthropic API key
// lives only in the backend's .env file.
window.CIVICVISION_API_BASE = "";
